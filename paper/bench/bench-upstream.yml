# Explicit-only override: point the anthropic adapter at the local fake upstream.
services:
  gateway:
    environment:
      MLPAL_ANTHROPIC_BASE_URL: http://host.docker.internal:9999
    extra_hosts:
      - "host.docker.internal:host-gateway"
